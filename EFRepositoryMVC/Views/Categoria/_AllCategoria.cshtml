@model List<EFRepositoryMVC.DTOs.Prenda.CategoriaCreacionDTO>

<select id="selectCategorias">
    @foreach (var categoria in Model)
    {
        <option value="@categoria.Name">@categoria.Name</option>
    }
</select>